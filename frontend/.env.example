# In local development, use the Vite proxy to avoid cross-origin requests.
VITE_API_BASE_URL=/api/v1
VITE_BACKEND_PROXY_TARGET=http://localhost:5000

# A deployed frontend can instead use a full API URL, provided the API allows
# the frontend origin through CORS.
