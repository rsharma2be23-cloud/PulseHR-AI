export function Pagination({ page, pageCount, onChange, total }) { if (pageCount < 2) return <p className="text-sm text-slate-500">{total} employee{total === 1 ? "" : "s"}</p>; return <div className="flex items-center justify-between gap-3 text-sm"><span className="text-slate-500">{total} employees · Page {page} of {pageCount}</span><div className="flex gap-2"><button disabled={page === 1} onClick={() => onChange(page - 1)} className="rounded-lg border border-slate-300 px-3 py-2 disabled:opacity-40">Previous</button><button disabled={page === pageCount} onClick={() => onChange(page + 1)} className="rounded-lg border border-slate-300 px-3 py-2 disabled:opacity-40">Next</button></div></div>; }

