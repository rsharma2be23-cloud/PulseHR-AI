export function FilterBar({ filters, onChange, departments }) { return <div className="flex flex-col gap-3 md:flex-row md:items-center"><select aria-label="Department filter" value={filters.department} onChange={(event) => onChange("department", event.target.value)} className="rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm"><option value="">All departments</option>{departments.map((department) => <option key={department._id} value={department._id}>{department.name}</option>)}</select><select aria-label="Role filter" value={filters.role} onChange={(event) => onChange("role", event.target.value)} className="rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm"><option value="">All roles</option>{["employee", "manager", "hr", "admin"].map((role) => <option key={role} value={role}>{role[0].toUpperCase() + role.slice(1)}</option>)}</select><select aria-label="Status filter" value={filters.employmentStatus} onChange={(event) => onChange("employmentStatus", event.target.value)} className="rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm"><option value="">All statuses</option><option value="active">Active</option><option value="notice_period">Notice period</option><option value="exited">Exited</option></select></div>; }

