import { useState } from "react";

export function FeedbackForm({ onSubmit, onCancel, isSubmitting }) { const [text, setText] = useState(""); const [error, setError] = useState(""); async function submit(event) { event.preventDefault(); if (!text.trim() || text.trim().length > 5000) { setError("Feedback must contain between 1 and 5000 characters."); return; } setError(""); await onSubmit({ text: text.trim() }, (fieldErrors) => setError(fieldErrors.form || "Unable to submit feedback.")); } return <form onSubmit={submit} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8">{error && <div className="mb-5 rounded-lg border border-rose-200 bg-rose-50 p-3 text-sm text-rose-800" role="alert">{error}</div>}<label className="block text-sm font-semibold text-slate-700">Your feedback<textarea value={text} onChange={(event) => setText(event.target.value)} maxLength={5000} rows={9} placeholder="Share a specific observation or suggestion…" className="mt-2 w-full resize-y rounded-lg border border-slate-300 px-3 py-2.5 font-normal outline-none focus:border-indigo-500" /></label><div className="mt-7 flex justify-end gap-3"><button type="button" onClick={onCancel} className="rounded-lg border border-slate-300 px-4 py-2.5 text-sm font-semibold">Cancel</button><button disabled={isSubmitting} className="rounded-lg bg-indigo-600 px-5 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{isSubmitting ? "Submitting…" : "Submit feedback"}</button></div></form>; }

